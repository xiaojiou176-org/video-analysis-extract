#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
# shellcheck source=./scripts/runtime/entrypoint.sh
source "$ROOT_DIR/scripts/runtime/entrypoint.sh"
vd_entrypoint_bootstrap tests smoke-computer-use-local "" "$@"
exec "$ROOT_DIR/scripts/ci/smoke_computer_use_local.sh" "$@"
