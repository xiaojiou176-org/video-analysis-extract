#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
# shellcheck source=./scripts/runtime/entrypoint.sh
source "$ROOT_DIR/scripts/runtime/entrypoint.sh"
vd_entrypoint_bootstrap infra apply-runner-startup-metadata "" "$@"
exec "$ROOT_DIR/scripts/deploy/apply_github_runner_startup_metadata.sh" "$@"
