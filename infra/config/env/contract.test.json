{
  "version": 1,
  "name": "test",
  "description": "Test-only environment variables.",
  "variables": [
    {
      "name": "API_INTEGRATION_DATABASE_URL",
      "scope": "test",
      "required": false,
      "secret": true,
      "default": "postgresql+psycopg://postgres:postgres@127.0.0.1:5432/postgres",
      "consumer": [
        "apps/api/tests/test_api_integration_smoke.py"
      ],
      "description": "Optional PostgreSQL admin DSN for ephemeral API integration smoke databases."
    }
  ]
}
