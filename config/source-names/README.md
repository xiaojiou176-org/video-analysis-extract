# Source Name Mappings

This directory stores repo-owned source-name mapping data used by application code.

- `subscriptions.up_names.json`: curated name mappings for source identifiers

Rules:

- Repo-owned configuration data may live here.
- Generic root-level `data/` storage is forbidden.
- Runtime outputs, probe results, and performance evidence must not be written here.
