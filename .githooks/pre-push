#!/usr/bin/env bash
set -euo pipefail

HOOK_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
ROOT_DIR="$(cd "$HOOK_DIR/.." && pwd)"
cd "$ROOT_DIR"

echo "[pre-push] quality gate (mode=pre-push)"
"$ROOT_DIR/scripts/strict_ci_entry.sh" \
  --mode pre-push \
  --heartbeat-seconds 20 \
  --ci-dedupe 0

echo "[pre-push] passed"
